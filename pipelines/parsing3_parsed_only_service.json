{
  "description": "parsing3_parsed_only_service: same as parsing2_full_service then drop event.original",
  "processors": [
    { "pipeline": { "name": "parsing2_full_service" } },
    { "remove":   { "field": "event.original", "ignore_missing": true } }
  ],
  "on_failure": [
    { "set": { "field": "error.message", "value": "{{{_ingest.on_failure_message}}}" } }
  ]
}
